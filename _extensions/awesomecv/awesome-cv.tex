%!TEX TS-program = xelatex
%!TEX encoding = UTF-8 Unicode
% Awesome CV LaTeX Template for CV/Resume
%
% This template has been downloaded from:
% https://github.com/posquit0/Awesome-CV
%
% Author:
% Claud D. Park <posquit0.bj@gmail.com>
% http://www.posquit0.com
%
%
% Adapted to be a Quarto template by Mitchell O'Hara-Wild
%
% Template license:
% CC BY-SA 4.0 (https://creativecommons.org/licenses/by-sa/4.0/)
%
%-------------------------------------------------------------------------------
% CONFIGURATIONS
%-------------------------------------------------------------------------------
$passoptions.latex()$
%
% A4 paper size by default, use 'letterpaper' for US letter
$doc-class.tex()$

% Configure page margins with geometry. The class has already loaded the
% package, so these are set with \geometry rather than as package options.
$if(geometry)$
\geometry{$for(geometry)$$geometry$$sep$,$endfor$}
$else$
\geometry{left=1.4cm, top=.8cm, right=1.4cm, bottom=1.8cm, footskip=.5cm}
$endif$

% Specify the location of the included fonts
\fontdir[fonts/]

% Color for highlights
% Awesome Colors: awesome-emerald, awesome-skyblue, awesome-red, awesome-pink, awesome-orange
%                 awesome-nephritis, awesome-concrete, awesome-darknight
$if(headcolor)$
\definecolor{awesome}{HTML}{$headcolor$}
$else$
\colorlet{awesome}{awesome-red}
$endif$

% Colors for text
% Uncomment if you would like to specify your own color
% \definecolor{darktext}{HTML}{414141}
% \definecolor{text}{HTML}{333333}
% \definecolor{graytext}{HTML}{5D5D5D}
% \definecolor{lighttext}{HTML}{999999}

% Set false if you don't want to highlight section with awesome color
\setbool{acvSectionColorHighlight}{true}

%-------------------------------------------------------------------------------
%	PERSONAL INFORMATION
%	Comment any of the lines below if they are not required
%-------------------------------------------------------------------------------
% Available options: circle|rectangle,edge/noedge,left/right
$if(profilepic)$
\photo$if(profilepic-options)$[$for(profilepic-options)$$profilepic-options$$sep$,$endfor$]$endif${$profilepic$}
$endif$
\name{$name$}{$if(surname)$$surname$$endif$}

$if(position)$
\position{$position$}
$endif$
$if(address)$
\address{$address$}
$endif$

$if(pronouns)$
\pronouns{$pronouns$}
$endif$
$if(phone)$
\mobile{$phone$}
$endif$
$if(email)$
\email{$email$}
$endif$
$if(www)$
\homepage{$www$}
$endif$
$if(orcid)$
\orcid{$orcid$}
$endif$
$if(publons)$
\publons{$publons$}
$endif$
$if(googlescholar)$
\googlescholar{$googlescholar$}
$endif$
$if(researchgate)$
\researchgate{$researchgate$}
$endif$
$if(github)$
\github{$github$}
$endif$
$if(gitlab)$
\gitlab{$gitlab$}
$endif$
$if(linkedin)$
\linkedin{$linkedin$}
$endif$
$if(twitter)$
\twitter{$twitter$}
$endif$
$if(skype)$
\skype{$skype$}
$endif$
$if(reddit)$
\reddit{$reddit$}
$endif$
$if(xing)$
\xing{$xing$}
$endif$
$if(extrainfo)$
\extrainfo{$extrainfo$}
$endif$

$if(aboutme)$
\quote{$aboutme$}
$endif$

%-------------------------------------------------------------------------------
%	PANDOC SUPPORT
%-------------------------------------------------------------------------------
% common.latex is required in every custom Quarto LaTeX template, and calls
% pandoc.tex, which in turn brings in syntax highlighting, tables.tex,
% graphics.tex, citations.tex, babel-lang.tex, tightlist.tex, biblio-config.tex
% and header-includes. Do not hand-copy any of those: they are Pandoc's to
% maintain, and a stale local copy silently drifts out of step with the reader.
$common.latex()$
$after-header-includes.latex()$
$hypersetup.latex()$

% Typography. font-settings.latex honours mainfont/sansfont/monofont and their
% *options and *fallback variants, so a user changes the CV's fonts the same way
% they would in any other Quarto pdf. fonts.latex is NOT called: it would load
% lmodern and unicode-math over the top of what the class has already set up.
$font-settings.latex()$

% The class does not read \rmfamily directly -- every style command goes through
% \bodyfont, \headerfont or \footerfont -- so \setmainfont above has no effect
% until those are pointed at it. Awesome-CV's own defaults (Source Sans Pro for
% the body, Roboto for the header) stand untouched when nothing is specified.
$if(mainfont)$
\renewcommand*{\bodyfont}{\rmfamily}
\renewcommand*{\bodyfontlight}{\rmfamily}
\renewcommand*{\footerfont}{\rmfamily}
% sourcesanspro is loaded with [default], which makes it \familydefault. Text
% that reaches the page outside one of the style commands above would otherwise
% stay Source Sans while everything around it changed.
\renewcommand*{\familydefault}{\rmdefault}
\normalfont
$endif$
$if(headerfont)$
% \setfontfamily redefines the family the class already declared with
% \newfontfamily. Without Path=, this resolves against installed system fonts
% rather than the bundled fonts/ directory.
\setfontfamily\headerfont[$for(headerfontoptions)$$headerfontoptions$$sep$,$endfor$]{$headerfont$}
\setfontfamily\headerfontlight[$for(headerfontoptions)$$headerfontoptions$$sep$,$endfor$]{$headerfont$}
$endif$

% Four further partials are deliberately not called here:
%   fonts.latex  see above.
%   before-title.tex, title.tex, before-body.tex, toc.tex  a CV has no title
%     block or contents page; \makecvheader below plays that role, and the
%     class's \section does not write \addcontentsline, so a toc would be empty.

\begin{document}

% Print the header with above personal informations
% Give optional argument to change alignment(C: center, L: left, R: right)
\makecvheader$if(header-align)$[$header-align$]$endif$

% Print the footer with 3 arguments(<left>, <center>, <right>)
% Leave any of these blank if they are not needed
% 2019-02-14 Chris Umphlett - add flexibility to the document name in footer, rather than have it be static Curriculum Vitae
$if(show_footer)$
\makecvfooter
  {$date$}
    {$name$$if(surname)$ $surname$$endif$~~~·~~~$if(docname)$$docname$$else$Curriculum Vitae$endif$}
  {\thepage$if(page_total)$~ of \pageref{LastPage}~$endif$}
$endif$


%-------------------------------------------------------------------------------
%	CV/RESUME CONTENT
%	Each section is imported separately, open each file in turn to modify content
%------------------------------------------------------------------------------

$if(linestretch)$
\setstretch{$linestretch$}
$endif$

$for(include-before)$
$include-before$
$endfor$

$body$

$before-bib.tex()$

$if(nocite-ids)$
\nocite{$for(nocite-ids)$$it$$sep$, $endfor$}
$endif$
$biblio.tex()$

$for(include-after)$
$include-after$
$endfor$

$after-body.tex()$

$if(page_total)$\label{LastPage}~$endif$
\end{document}
